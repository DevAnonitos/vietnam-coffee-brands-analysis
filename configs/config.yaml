# default config
defaults:
  - db: default
  - data: default
  - paths: default
  - tests: default
  - models: default
  - logging: default
  - features: default
  - training: default
  - evaluation: default
  - api: default
  - _self_

# metadata project config
project:
  name: "Vietnam Coffee Branding Analysis"
  version: "1.0.0"
  description: "Advanced data analysis and machine learning for Vietnam coffee brands"
  author: "Bao Nguyen Duc Gia"
  seed: 42

# environment configuration
env: local
debug: false

# Experiment tracking flags
tracking:
  use_mlflow: false
  use_wandb: false
  experiment_name: "vietnam-coffee-brands"
  run_name: "baseline-run"
