# root config
root: ${hydra:runtime.cwd}

data:
  root: ${paths.root}/data

src:
  root: ${paths.root}/src

logs:
  root: ${paths.root}/logs
  training: ${paths.root}/logs/training
  evaluation: ${paths.root}/logs/evaluation

models:
  root: ${paths.root}/model

reports:
  root: ${paths.root}/reports
  figures: ${paths.root}/reports/figures
  metrics: ${paths.root}/reports/metrics
  html: ${paths.root}/reports/html
  shap: ${paths.root}/reports/shap

tests:
  root: ${paths.root}/tests
  unit: ${paths.root}/tests/unit
  integration: ${paths.root}/tests/integration

scripts:
  root: ${paths.root}/scripts
